# Install on a CUDA GPU machine (or Google Colab) for fine-tuning.
# These are NOT needed to run the LangGraph app itself.
torch>=2.3.0
transformers>=4.46.0
datasets>=3.0.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
sentencepiece>=0.2.0

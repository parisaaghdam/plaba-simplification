# Install on a CUDA GPU machine (Colab / HPC) for fine-tuning.
# These are NOT needed to run the LangGraph app itself.
# Versions are pinned to a known-good, mutually compatible set.
torch>=2.4.0
transformers==4.55.2
datasets==3.6.0
trl==0.21.0
peft==0.15.2
accelerate==1.8.1
bitsandbytes==0.46.1
sentencepiece>=0.2.0
